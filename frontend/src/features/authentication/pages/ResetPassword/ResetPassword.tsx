import Layout from '../../../../components/layout/Layout';
import classes from './ResetPassword.module.scss';

const ResetPassword = () => {
  return (
    <Layout><div className={classes.root}>
    ResetPassword
  </div></Layout>
  );
};

export default ResetPassword;   