spring:
  application:
    name: backend
  datasource:
    url: 'jdbc:mysql://localhost:3306/linkedin?createDatabaseIfNotExist=true'
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

jwt:
  signerKey: "16092005"


